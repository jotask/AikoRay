#pragma once

namespace raylib
{
    #include <raylib.h>
}

namespace aiko
{

    using Vector2 = raylib::Vector2;
    using Vector3 = raylib::Vector3;
    using Vector4 = raylib::Vector4;
    using Matric = raylib::Matrix;
    using Quaterion = raylib::Quaternion;
    using Color = raylib::Color;

}